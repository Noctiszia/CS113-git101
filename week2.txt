Week 2
1. การใช้งานตัวแปร
ภาษา C ต้องมีการประกาศตัวแปรจึงจะสามารถใช้งานตัวแปรได้ เช่น int x = 15;
char c;	float i; 
	1.1 ชื่อตัวแปร 
	ประกอบด้วย อักขระและตัวเลข   //อักษรตัวแรกต้องเป็นอักขระ รวมทั้ง _
				             //พิมพ์ใหญ่และพิมพ์เล็กเป็นอักขระที่ต่างกัน
	//ห้ามตัวแปรซ้ำกับคีย์เวิร์ด เช่น if, else, float, double char
	1.2 การตั้งชื่อตัวแปร
	ควรตั้งใช้เข้าใจง่าย และชัดเจนตามจุดประสงค์ของการใช้งาน
2. ประเภทข้อมูลและขนาด
ข้อมูลที่เก็บเลขจำนวนเต็ม
		char 	//ใช้หน่วยความจำ 1 ไบท์
		int 	//ส่วนใหญ่ใช้ 4 ไบท์
		float	 //ใช้ 4 ไบท์
		double 	//ใช้ 8 ไบท์
short ลดพื้นที่จัดเก็บ long เพิ่มพื้นที่จัดเก็บ	       //สามารถใช้ long long ได้ด้วย
signed ใช้กำหนดประเภทตัวแปรที่จัดเก็บในจำนวนเต็ม สามารถเก็บค่าที่มีเครื่องหมายได้
3. ค่าคงที่
จำนวนเต็ม เช่น integer	   //15
ค่าคงที่จำนวนจริง เช่น floating-point	//5.56
	เลขฐาน2      //0b หรือ 0B บ่งบอกว่าเป็นฐาน2
	เลขฐาน8      //0 แล้วตามด้วยตัวเลขจำนวนเต็ม ใช้ %o ในการ print format
	เลขฐาน16    //ใช้ 0x หรือ 0X จะบ่งบอกถึงฐาน16 ใช้ %x ในการ print format
	ตัวอักษรจะอยู่ระหว่าง single quote
	ตัวเลขและ ASCII สามารถแปลงถึงกันได้
	String อยู่ระหว่าง double quote	  //"" เป็น string ว่าง
4. การประกาศตัวแปร	//ต้องประกาศก่อนถูกใช้งาน และต้องระบุประเภทข้อมูล
	4.1 ใช้ printf() และ puts() ในการพิมพ์ format ของ printf() เช่น %d, %f
	4.2 Input Output
	getchar() รับ 1 อักขระจาก stdin
	4.3 นิพจน์
	ประกอบด้วยอย่างน้อย 1 operator และอย่างน้อย 1 operand แต่สามารถอยู่ในรูปค่าคงที่ได้
5. ตัวดำเนินการทางคณิตศาสตร์
+, -, *, /, %	//% ไม่สามารถใช้กับ float และ double ได้
ถ้า / ใช้กับ integer และ integer ผลลัพธ์จะเป็น integer ที่ถูกปัดเศษทิ้ง
+, - สามารถเป็น unary operator ได้
หากมี precedence เท่ากัน ลำดับการทำงานจะทำจากซ้ายไปขวา
6. Relational Operators >, >=, <, <=, ==, !=
    Logical Operators &&, ||, !
6.1 Comma Operator    //statement จะอยู่ในรูปนิพจน์ที่ปิดท้ายด้วย ; สามารถใช้ comma 
คั่นระหว่างนิพจน์ได้
//สามารถใช้ Increment และ Decrement Operators เช่น n++, ++n, n--, --n
7. Bitwise Operators
	& (and), | (or), ^ (exclusive OR), << (left shift), >> (right shift), ~ (one’s complement)
//x & y กับ x && y ไม่เหมือนกัน 
